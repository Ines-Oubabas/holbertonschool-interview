# Radix Sort

This project implements the LSD Radix Sort algorithm in C.

## Files

- `sort.h` : header file containing function prototypes
- `0-radix_sort.c` : implementation of radix sort

## Prototype

```c
void radix_sort(int *array, size_t size);