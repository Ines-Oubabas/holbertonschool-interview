# Substring

## Description
This project implements a function that finds all the possible starting indices of substrings in a string that are a concatenation of a list of words exactly once and without any intervening characters.

## Prototype
```c
int *find_substring(char const *s, char const **words, int nb_words, int *n);
Files
substring.c
substring.h
