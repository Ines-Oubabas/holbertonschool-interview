# AVL Trees

This project contains a function that checks if a binary tree is a valid AVL Tree.

## Prototype

int binary_tree_is_avl(const binary_tree_t *tree);

## Description

An AVL Tree is a binary search tree where the difference between the heights
of the left and right subtrees of any node is not greater than 1.
